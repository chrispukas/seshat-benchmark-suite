import os

from typing import Optional, Any, Tuple, List, Dict


def crawl_single(endpoint_subpath: str
                 ) -> List[str]:
    
    dirs: str = os.listdir(endpoint_subpath)
    


def get_endpoints_local(endpoint_root: str
                        ) -> List[str]:
    pass


root: str = \
    "/Users/apple/Documents/github/neurips_llms/llm-bechmark/db/seshat/modules"

get_endpoints_local(endpoint_root=root)
